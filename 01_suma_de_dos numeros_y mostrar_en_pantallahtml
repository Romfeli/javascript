<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejercicio 1</title>
</head>
<body>
    <script>
        // Paso 1: Solicitar el primer número al usuario
        //var numero1 = prompt("Ingresa el primer número:");

        // Paso 2: Convertir el valor ingresado a un número
        //numero1 = parseFloat(numero1);

        // Paso 3: Solicitar el segundo número al usuario
        //var numero2 = prompt("Ingresa el segundo número:");

        // Paso 4: Convertir el valor ingresado a un número
        //numero2 = Number(numero2);


        var numero1 = parseFloat(prompt("introduce el primer numero"))
        var numero2 = Number(prompt("introduce el segundo numero"))
        // Paso 5: Sumar los dos números
        var resultado = numero1 + numero2;

        // Mostrar el resultado en un cuadro de diálogo
        //alert("El resultado de la suma es: " + resultado);
        alert("La suma de: " + numero1 + " y " + numero2 + " es " + (numero1+numero2))
        // Mostrar el resultado en la consola (opcional)
        console.log("El resultado de la suma es: " + resultado);

        document.write("El primer numero fue: " + numero1 + "<br>" )
        document.write("El segundo numero fue: " + numero2 + "<br>")
        document.write("La suma de los numeros es: " + resultado)
    </script>
    
</body>
</html>